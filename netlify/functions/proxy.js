const http=require('node:http');const https=require('node:https');
const MAX_REDIRECTS=6,TIMEOUT_MS=15000;
function validTarget(value){let u;try{u=new URL(value)}catch{return null}if(!['http:','https:'].includes(u.protocol))return null;const host=u.hostname.toLowerCase();if(host==='localhost'||host==='127.0.0.1'||host==='0.0.0.0'||host==='::1'||host.endsWith('.localhost'))return null;return u}
function fetchUrl(target,redirects=0){return new Promise((resolve,reject)=>{if(redirects>MAX_REDIRECTS)return reject(new Error('Too many redirects.'));const url=validTarget(target);if(!url)return reject(new Error('Invalid or unsupported URL.'));const transport=url.protocol==='https:'?https:http;const req=transport.get(url,{timeout:TIMEOUT_MS,headers:{'User-Agent':'Mozilla/5.0 (compatible; StudySpaceProxy/1.0)','Accept':'text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,*/*;q=0.8','Accept-Encoding':'identity'}},res=>{const location=res.headers.location;if(location&&[301,302,303,307,308].includes(res.statusCode)){res.resume();return fetchUrl(new URL(location,url).href,redirects+1).then(resolve).catch(reject)}const chunks=[];res.on('data',c=>chunks.push(c));res.on('end',()=>resolve({statusCode:res.statusCode||200,headers:res.headers,body:Buffer.concat(chunks),url:url.href}))});req.on('timeout',()=>req.destroy(new Error('Upstream request timed out.')));req.on('error',reject)})}
function proxied(value,base){try{const u=new URL(value,base);if(!['http:','https:'].includes(u.protocol))return value;return '/proxy?url='+encodeURIComponent(u.href)}catch{return value}}
function rewriteHtml(html,base){html=html.replace(/\b(href|src|action|poster|data)\s*=\s*(["'])(.*?)\2/gi,(m,a,q,v)=>{if(/^\s*(?:#|javascript:|mailto:|tel:|data:|blob:)/i.test(v))return m;return `${a}=${q}${proxied(v,base)}${q}`});html=html.replace(/\bsrcset\s*=\s*(["'])(.*?)\1/gi,(m,q,v)=>{const parts=v.split(',').map(x=>{const p=x.trim().split(/\s+/);if(p[0])p[0]=proxied(p[0],base);return p.join(' ')});return `srcset=${q}${parts.join(', ')}${q}`});html=html.replace(/<meta[^>]+http-equiv\s*=\s*["']?Content-Security-Policy[^>]*>/gi,'');html=html.replace(/<meta[^>]+http-equiv\s*=\s*["']?X-Frame-Options[^>]*>/gi,'');return html}
exports.handler=async event=>{const target=event.queryStringParameters?.url;if(!target)return{statusCode:400,headers:{'Content-Type':'text/plain'},body:'Missing url parameter.'};try{const result=await fetchUrl(target);let body=result.body;let contentType=result.headers['content-type']||'text/plain';if(/text\/html|application\/xhtml\+xml/i.test(contentType)){body=Buffer.from(rewriteHtml(body.toString('utf8'),result.url),'utf8');contentType='text/html; charset=utf-8'}return{statusCode:result.statusCode,headers:{'Content-Type':contentType,'Cache-Control':'no-store','Access-Control-Allow-Origin':'*','X-Content-Type-Options':'nosniff'},body:body.toString('base64'),isBase64Encoded:true}}catch(error){return{statusCode:502,headers:{'Content-Type':'text/plain; charset=utf-8','Access-Control-Allow-Origin':'*'},body:`Proxy error: ${error.message}`}}};
